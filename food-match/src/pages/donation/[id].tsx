import { Box } from '@chakra-ui/react'
import { Navbar } from '~/features/common/components'

const donationItem = () => {
  return (
    <>
      <Navbar />
      Data to be pulled using router.query when the GET tRPC route is set up for
      individual donations.
    </>
  )
}

export default donationItem
